import { UpdateClientProfileDto } from './update-client-profile.dto';

describe('UpdateClientProfileDto', () => {
  it('should be defined', () => {
    expect(new UpdateClientProfileDto()).toBeDefined();
  });
});
