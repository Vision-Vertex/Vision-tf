import { UpdateDeveloperProfileDto } from './update-developer-profile.dto';

describe('UpdateDeveloperProfileDto', () => {
  it('should be defined', () => {
    expect(new UpdateDeveloperProfileDto()).toBeDefined();
  });
});
